# Set the registry to the default npm registry
registry=https://registry.npmjs.org/

# Set the authentication token for the registry (replace <TOKEN> with your actual token)
//registry.npmjs.org/:_authToken=<TOKEN>

# Disable package-lock generation
package-lock=true

# Enable strict SSL
strict-ssl=true