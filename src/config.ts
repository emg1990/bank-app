export const BASE_URL = process.env.REACT_APP_BASE_URL || 'http://localhost:3001';

// Mock ownerId, this should be obtained from session id
export const OWNER_ID = 1;

// Define the number of decimals allowed
export const ALLOWED_DECIMALS = 2;